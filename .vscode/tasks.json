{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run All Tests",
            "type": "shell",
            "windows":{
                "command": "python -m ${workspaceFolder}\\test discover -v"
            },
        }
    ]
}